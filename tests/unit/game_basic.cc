#include "gtest/gtest.h"

namespace az::game::xq {
namespace {

TEST(GameBasic, GetBoardReturnsInitialBoard) {
  GTEST_SKIP() << "TODO(TASK-GAME-BASIC-TEST): verify the initial board state.";
}

TEST(GameBasic, CurrentRoundStartsAtZero) {
  GTEST_SKIP() << "TODO(TASK-GAME-BASIC-TEST): verify the initial round.";
}

TEST(GameBasic, CurrentPlayerMatchesStartingPlayer) {
  GTEST_SKIP() << "TODO(TASK-GAME-BASIC-TEST): verify the starting player.";
}

TEST(GameBasic, LastPlayerStartsEmpty) {
  GTEST_SKIP()
      << "TODO(TASK-GAME-BASIC-TEST): verify LastPlayer() before any action.";
}

TEST(GameBasic, LastActionStartsEmpty) {
  GTEST_SKIP()
      << "TODO(TASK-GAME-BASIC-TEST): verify LastAction() before any action.";
}

}  // namespace
}  // namespace az::game::xq
