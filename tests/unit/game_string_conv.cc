#include "gtest/gtest.h"

namespace az::game::xq {
namespace {

TEST(GameStringConv, BoardReadableString) {
  GTEST_SKIP()
      << "TODO(TASK-GAME-STR-TEST): verify the human-readable board string.";
}

TEST(GameStringConv, ActionFromString) {
  GTEST_SKIP()
      << "TODO(TASK-GAME-STR-TEST): verify parsing supported action strings.";
}

TEST(GameStringConv, ActionToString) {
  GTEST_SKIP()
      << "TODO(TASK-GAME-STR-TEST): verify formatting supported actions.";
}

}  // namespace
}  // namespace az::game::xq
